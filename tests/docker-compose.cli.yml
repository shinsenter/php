version: '3'
services:

  ##############################################################################

  cli:
    image: shinsenter/php:${PHP_VERSION:-8.1}-cli
    extends:
      file: common.yml
      service: common-php
    build:
      context: ../src/php/cli
      args:
        BASE_IMAGE: shinsenter/s6-ubuntu:latest

################################################################################
