version: '3'
services:

  ##############################################################################

  fpm:
    image: shinsenter/php:${PHP_VERSION:-8.1}-fpm
    extends:
      file: common.yml
      service: common-php
    build:
      context: ../src/php/fpm
      args:
        BASE_IMAGE: shinsenter/php

################################################################################
