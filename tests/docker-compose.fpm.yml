version: '3'
services:

  ##############################################################################

  fpm:
    extends:
      file: common.yml
      service: common-php
    image: shinsenter/php:${PHP_VERSION:-8.1}-fpm
    build:
      context: ../src/php/fpm

################################################################################
