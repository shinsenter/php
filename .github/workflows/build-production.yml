name: Publish Docker (shinsenter/php)

################################################################################
################################################################################

on:
  schedule:
    - cron: "0 0 * * 2"
  push:
    branches:
      - main

################################################################################
################################################################################

jobs:
  ##############################################################################
  # Base Ubuntu with S6 Overlay
  ##############################################################################

  base_image:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        base:
          - ubuntu
        version:
          - "20.04"
          - focal
          - latest
    env:
      DOCKER_BUILDKIT: "1"
      OS_BASE: ${{ matrix.base }}
      OS_VERSION: ${{ matrix.version }}
    steps:
      - uses: actions/checkout@v2
        with:
          ref: main
      - name: Set up Docker
        uses: docker/setup-buildx-action@v1
      - name: Restore cache layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Create cache path
        run: |
          mkdir -p /tmp/.buildx
          docker buildx create --use
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          platforms: linux/amd64
          tags: shinsenter/s6-ubuntu:${{ matrix.version }}
          context: "{{defaultContext}}:src/base/"
          cache-from: type=local,src=/tmp/.buildx
          cache-to: type=local,dest=/tmp/.buildx-new,mode=max
          push: true
          pull: true
      - name: Swap cache path
        run: |
          rm -rf /tmp/.buildx
          mv /tmp/.buildx-new /tmp/.buildx

  ##############################################################################
  # Base PHP-CLI
  ##############################################################################

  php-cli:
    needs: base_image
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 3
      matrix:
        variation:
          - cli
        version:
          - "7.4"
          - "8.0"
          - "8.1"
    env:
      PHP_VERSION: ${{ matrix.version }}
      IMAGE_BASE: shinsenter/s6-ubuntu:latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: main
      - name: Set up Docker
        uses: docker/setup-buildx-action@v1
      - name: Restore cache layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Create cache path
        run: |
          mkdir -p /tmp/.buildx
          docker buildx create --use
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          platforms: linux/amd64
          tags: shinsenter/php:${{ matrix.version }}-${{ matrix.variation }}
          context: "{{defaultContext}}:src/php/${{ matrix.variation }}/"
          cache-from: type=local,src=/tmp/.buildx
          cache-to: type=local,dest=/tmp/.buildx-new,mode=max
          push: true
          pull: true
      - name: Swap cache path
        run: |
          rm -rf /tmp/.buildx
          mv /tmp/.buildx-new /tmp/.buildx

  ##############################################################################
  # Base PHP-FPM
  ##############################################################################

  php-fpm:
    needs: php-cli
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 3
      matrix:
        variation:
          - fpm
        version:
          - "7.4"
          - "8.0"
          - "8.1"
    env:
      PHP_VERSION: ${{ matrix.version }}
      IMAGE_BASE: shinsenter/php
    steps:
      - uses: actions/checkout@v2
        with:
          ref: main
      - name: Set up Docker
        uses: docker/setup-buildx-action@v1
      - name: Restore cache layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Create cache path
        run: |
          mkdir -p /tmp/.buildx
          docker buildx create --use
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          platforms: linux/amd64
          tags: shinsenter/php:${{ matrix.version }}-${{ matrix.variation }}
          context: "{{defaultContext}}:src/php/${{ matrix.variation }}/"
          cache-from: type=local,src=/tmp/.buildx
          cache-to: type=local,dest=/tmp/.buildx-new,mode=max
          push: true
          pull: true
      - name: Swap cache path
        run: |
          rm -rf /tmp/.buildx
          mv /tmp/.buildx-new /tmp/.buildx

  ##############################################################################
  # Base PHP Webservers (Apache or Nginx)
  ##############################################################################

  php-web:
    needs: php-fpm
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 3
      matrix:
        variation:
          - fpm-apache
          - fpm-nginx
        version:
          - "7.4"
          - "8.0"
          - "8.1"
    env:
      PHP_VERSION: ${{ matrix.version }}
      IMAGE_BASE: shinsenter/php
    steps:
      - uses: actions/checkout@v2
        with:
          ref: main
      - name: Set up Docker
        uses: docker/setup-buildx-action@v1
      - name: Restore cache layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Create cache path
        run: |
          mkdir -p /tmp/.buildx
          docker buildx create --use
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          platforms: linux/amd64
          tags: shinsenter/php:${{ matrix.version }}-${{ matrix.variation }}
          context: "{{defaultContext}}:src/php/${{ matrix.variation }}/"
          cache-from: type=local,src=/tmp/.buildx
          cache-to: type=local,dest=/tmp/.buildx-new,mode=max
          push: true
          pull: true
      - name: Swap cache path
        run: |
          rm -rf /tmp/.buildx
          mv /tmp/.buildx-new /tmp/.buildx

################################################################################
################################################################################
